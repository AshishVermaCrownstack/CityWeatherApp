import {CityInitialStateProps} from '../../typings/typings';

const cityInitialState: CityInitialStateProps = {
  loading: false,
  cityList: [],
  totalPage: 1,
  nextPage: null,
  currentPage: 1,
};

export default cityInitialState;
